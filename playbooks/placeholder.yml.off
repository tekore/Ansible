---
- name: Configure New Server
  hosts: 
  gather_facts: yes
  remote_user: ubuntu
  connection: 22
  roles:
    - general
    - fail2ban
    - LXD
  vars_files:
    - ./../roles/vars/vars.yml
  vars:
    ansible_ssh_private_key_file: 
